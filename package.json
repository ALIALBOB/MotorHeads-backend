{
  "name": "motorheads-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MotorHeads registry and agent API for owner-edited NFT visuals.",
  "scripts": {
    "check": "node --check src/index.js && node --check src/state.js && node --check src/chainState.js && node --check src/responses.js && node --check src/contracts.js && node --check src/parts.js && node --check scripts/smoke-test.mjs && node --check scripts/dev-server.mjs",
    "test": "node scripts/smoke-test.mjs",
    "dev:node": "node scripts/dev-server.mjs",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "db:migrate:local": "npx wrangler d1 execute motorheads_registry --local --file=schema.sql",
    "db:migrate:prod": "npx wrangler d1 execute motorheads_registry --remote --file=schema.sql"
  },
  "engines": {
    "node": ">=20"
  }
}
